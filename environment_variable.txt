common lib:

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <string.h>

common structure:
    int main(int argc, char *argv[]) {

        // error check
        if (argc != 3) {
            printf("0\n");
            return 1;
        }
        
        // usually there's two arguments
        char *env1 = getenv(argv[1]);
        char *env2 = getenv(argv[2]);

        // common use strcmp(env1, env2) to compare the value of the environs 
    }
